{
  "name": "meta-governor",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/engine",
    "packages/dashboard",
    "packages/shared"
  ],
  "scripts": {
    "audit": "npm run audit --workspace=packages/engine",
    "build": "npm run build --workspace=packages/engine"
  }
}