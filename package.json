{
  "name": "meta-governor",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/engine",
    "packages/dashboard",
    "packages/shared",
    "packages/api"
  ],
  "scripts": {
    "audit": "npm run audit --workspace=packages/engine",
    "build": "npm run build --workspace=packages/engine",
    "api": "npm run dev --workspace=packages/api"
  }
}